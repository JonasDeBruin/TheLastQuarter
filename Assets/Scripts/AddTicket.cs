using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AddTicket : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GamemanagerMainGame.ticketCount++;
    }
}
